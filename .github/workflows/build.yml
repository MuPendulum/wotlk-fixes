name: Build Artifacts

on:
  workflow_dispatch:
  push:
    tags:
      - '*.*.*'

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            suffix: .exe
            archive_ext: zip

          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            suffix: ""
            archive_ext: tar.gz

    env:
      TARGET: ${{ matrix.target }}
      ARTIFACT_DIR: client-patcher-${{ matrix.target }}

    steps:
      - uses: actions/checkout@v6

      - name: Install musl-tools
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends musl-tools

      - name: Setup Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          target: ${{ matrix.target }}

      - name: Build
        run: cargo build --frozen --release --target ${{ matrix.target }}

      - name: Prepare artifacts
        shell: bash
        run: |
          mkdir -p "$ARTIFACT_DIR"
          cp UNLICENSE "$ARTIFACT_DIR/"
          cp THIRD-PARTY-NOTICES "$ARTIFACT_DIR/"
          cp README.md "$ARTIFACT_DIR/"
          cp "target/$TARGET/release/client-patcher${{ matrix.suffix }}" "$ARTIFACT_DIR/"

      - name: Package artifacts
        shell: bash
        run: |
          if [[ "${{ matrix.archive_ext }}" == "zip" ]]; then
            7z a -tzip -mfb=258 -mpass=15 "${ARTIFACT_DIR}.zip" "$ARTIFACT_DIR"
          else
            tar -cf "${ARTIFACT_DIR}.tar" "$ARTIFACT_DIR"
            7z a -tgzip -mfb=258 -mpass=15 "${ARTIFACT_DIR}.tar.gz" "${ARTIFACT_DIR}.tar" -sdel
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          path: ${{ env.ARTIFACT_DIR }}.${{ matrix.archive_ext }}
          archive: false
