[package]
name = "client-patcher"
version = "0.1.1"
edition = "2024"
license = "Unlicense"

[dependencies]

[profile.release]
opt-level = "s"
strip = "symbols"
lto = true
panic = "abort"
codegen-units = 1
